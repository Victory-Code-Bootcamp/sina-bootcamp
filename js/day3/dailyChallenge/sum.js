function sum(a, b) {
    return a + b;
}

module.exports = sum;

// function sortArray(arr) {
//     return arr.sort((a, b) => a - b); // Sort in ascending order
//   }
  
//   const arr = [3, 8, 7, 6, 5, -4, 3, 2, 1];
//   console.log(sortArray(arr)); // Output: [-4, 1, 2, 3, 3, 5, 6, 7, 8]
